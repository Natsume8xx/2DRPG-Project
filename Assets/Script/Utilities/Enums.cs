public enum NPCState
{
    Patrol,Chase,Skill
}

public enum SceneType
{
    Location,Menu
}

public enum PresistentType
{
    ReadWrite,DoNotPresistent
}

public enum InventoryType
{
    Bag,CharacterState,Action
}
